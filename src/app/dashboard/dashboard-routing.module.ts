import { Routes } from "@angular/router";
import { DashboardComponent } from "./dashboard.component";

export const DashboardaRoutes : Routes = [
    {
        path: 'dashboard',
        component: DashboardComponent
    }
]