import { Routes } from "@angular/router";
import {CalculadoraComponent} from "./componets/calculadora.component"

export const calculadoraRoutes : Routes = [
    {
        path: 'calculadora',
        component: CalculadoraComponent
    }
]