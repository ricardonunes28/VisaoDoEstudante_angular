import { Routes } from "@angular/router";
import { DataBindingsComponentComponent } from "./data-bindings-component";

export const dataBindingsRoutes : Routes = [
    {
        path: 'data-bindings',
        component: DataBindingsComponentComponent
    }
]